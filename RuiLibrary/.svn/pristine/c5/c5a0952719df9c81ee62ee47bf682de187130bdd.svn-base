package com.yixin.nfyh.cloud.dialog;

import java.util.List;

import android.content.Context;

import com.yixin.nfyh.cloud.model.view.DialogViewModel;

public class DateDialog extends NumberSelectorDialog
{

	public DateDialog(Context context, DialogPopupWindowListener listener,
			List<DialogViewModel> model)
	{
		super(context, listener, model);
	}

	@Override
	protected void onCreate(List<DialogViewModel> model)
	{
		// 有且只有一个

		super.onCreate(model);
	}

}
